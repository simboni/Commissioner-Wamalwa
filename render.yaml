# Render Blueprint — static site (plain HTML/CSS/JS, no build step)
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: commissioner-wamalwa
    runtime: static
    buildCommand: "echo no build needed"
    staticPublishPath: .
